[tool.poetry]
name = "tables"
version = "0.0.1"
description = "Tables"
authors = ["A O <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
